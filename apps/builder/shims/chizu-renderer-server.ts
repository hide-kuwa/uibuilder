// append-only: server-safe shim for API routes
// Import directly from the codegen module to avoid pulling runtime/React context on server
export { generatePageCode } from '../../../packages/chizu-renderer/src/codegen'

