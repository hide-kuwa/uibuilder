# Travel App

This project contains sample components for expense tracking and a cloud function
for settlement calculations using Firebase.

## Environment Variables

Copy `.env.example` to `.env.local` and fill in the values. `NEXT_PUBLIC_RAKUTEN_APP_ID`
is the developer ID for the Rakuten Travel API which is required for the hotel
suggester component.
